User
    id                      int
    email                   string
    username                string
    firstName               string
    lastName                string
    salt                    string
    hashed-password         string
    position (Position)     int (fk)
    * password              string
    * fullname              string

Position
    id                      int
    name                    string

Project
    id                      int
    icon                    string
    title                   string
    creation-date           date
    key                     string
    type                    string
    type-icon               string
    lead (User)             int (fk)

Issue
    id                      int
    key                     string
    create-by (User)        int (fk)
    creation-date           date
    deadline                date
    status (Status)         int (fk)
    label (Labels)          int (fk)
    assigned-to: (User)     int (fk)
    title                   string
    description             BLOB

Status
    id                      int
    name                    string
    color                   string